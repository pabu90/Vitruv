platform: linux

image_resource:
  type: docker-image
  source:
    repository: maven
    tag: 3.3.9-jdk-8

inputs:
  - name: vitruv-git-repo

run:
  path: sh
  args:
  - -exc
  - |
    apt-get update
    apt-get install -y xvfb
    Xvfb :99
    export DISPLAY=:99
    sleep 10s
    mvn -f vitruv-git-repo/pom.xml clean install

params:
  MAVEN_BASEDIR: vitruv-git-repo
